
import React from 'react';
import ReactDOM from 'react-dom/client';
import AgreementApprovalPortal from './components/AgreementApprovalPortal';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AgreementApprovalPortal />
  </React.StrictMode>
);
